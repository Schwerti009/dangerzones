# Python ETL

The ETL pipeline is intentionally deferred to Phase 3. Its lockfile, `ruff`, `mypy --strict`, adapters, and snapshot tests will be added together with the first production-bound transformation.
